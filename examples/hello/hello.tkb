// AArch64 ベアメタル Hello World。UART への文字列送信の最小実装。
fn uart_putc(c: char) {
    let dr: *io char = 0x09000000; // PL011 UART DR: 書き込みで送信
    *dr = c;
}

fn uart_puts(s: *char) {
    while (*s != 0) {
        uart_putc(*s);
        s = s + 1;
    }
}

fn main() {
    uart_puts("Hello, World!\n");
}
