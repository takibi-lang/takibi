fn uart_putc(c: char): void {
    let dr: *char = 0x09000000;
    *dr = c;
}

fn uart_puts(s: *char): void {
    while (*s != 0) {
        uart_putc(*s);
        s = s + 1;
    }
}

fn main(): void {
    uart_puts("Hello, World!\n");
}
