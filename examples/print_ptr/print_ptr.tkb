// Demo of casting a pointer value to i32 with as and displaying it in hex.

fn main() {
    uart_puts("=== print_ptr test ===\r\n");

    let uart: *io u8 = 0x09000000;
    uart_puts("uart: ");
    uart_print_hex(uart as i32);
    uart_puts("\r\n");

    uart_puts("done\r\n");
}

