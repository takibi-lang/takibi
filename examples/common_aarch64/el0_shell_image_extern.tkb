// extern fn declarations for examples/common_aarch64/el0_shell_image.S
// (GitHub issue #156), shared by the legacy RPi3 and maintained RPi5
// example builds.

extern fn el0_shell_image_addr() -> usize;
extern fn el0_shell_image_len() -> usize;
