// extern fn declarations for examples/common_rpi5/dma_asm.S (GitHub
// issue #67 follow-up, RP1 USB/XHCI DMA buffer cache maintenance),
// split into its own file matching examples/common_rpi5/
// tlb_asm_extern.tkb's own reasoning.

extern fn rpi5_dcache_clean_range(va: usize, len: usize);
extern fn rpi5_dcache_invalidate_range(va: usize, len: usize);
extern fn rpi5_dcache_invalidate_only_range(va: usize, len: usize);
