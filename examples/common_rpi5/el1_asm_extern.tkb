// extern fn declaration for examples/common_rpi5/el1_asm.S's
// rpi5_el1_enter, split into its own file matching
// examples/common_rpi3/el1_asm_extern.tkb's own reasoning (a duplicate
// `extern fn` declaration is a hard conflict if two files needing it
// both declared it locally).

extern fn rpi5_el1_enter(el1_sp: usize) !{noreturn};
