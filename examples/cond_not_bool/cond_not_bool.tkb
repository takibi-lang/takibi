// This file is expected to produce a compile error.
// if/while/&&/|| conditions require bool -- no C-style implicit
// int-truthy coercion. A bare integer literal's inferred type is an
// unconstrained type variable that would otherwise silently bind to
// TBool; this is rejected explicitly instead of being accepted and
// crashing at codegen. See CLAUDE.md's bool-only-condition decision
// (matches Rust/Zig, not C) for the full reasoning.
fn app_main() -> i32 {
    while (1) {
    }
    return 0;
}
