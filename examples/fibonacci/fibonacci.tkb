// Fibonacci sequence (iterative). Prints the first 10 terms.

fn main() {
    let mut a: i32 = 0;
    let mut b: i32 = 1;
    let mut tmp: i32 = 0;
    for i in 0..<10 {
        uart_print_uint(b);
        uart_puts("\r\n");
        tmp = a + b;
        a = b;
        b = tmp;
    }
}

