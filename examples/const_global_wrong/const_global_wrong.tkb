// This file is expected to produce a compile error.
// A plain global `let` is an immutable compile-time constant: reassigning it
// is rejected, just like a local `let` variable. Use `let mut` if the
// global needs to be written to at runtime.
let QUEUE_SIZE: i32 = 4;
fn app_main() {
    QUEUE_SIZE = 8;
}
