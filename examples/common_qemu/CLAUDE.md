# Claude Code Compatibility

This directory uses `AGENTS.md` as the canonical agent guidance file.

Read the repository root `AGENTS.md` first, then read this directory's
`AGENTS.md` for QEMU/AArch64-specific reference material.
