growable_pool
pages consumed before first insert: 0
chunks active after filling one chunk (want 1): 1
chunks active after crossing a chunk boundary (want 2): 2
pages consumed by pool_a alone (want 2): 2
cross-chunk values distinct and unaliased: true
stale handle rejected immediately after remove: true
stale handle still rejected after reuse (proves generation, not mere occupancy, gates the check): true
double free of a2's already-freed slot counted: true
pool_c hit its own MAX_CHUNKS ceiling while the page provider still had a page free: true
pages consumed once the provider's own pool is exhausted (want 4): 4
pool_b hit the underlying page provider's ceiling, not its own (own chunks used < own MAX_CHUNKS): true
done
