growable_pool
pages consumed before first insert: 0
chunks active after filling one chunk (want 1): 1
chunks active after crossing a chunk boundary (want 2): 2
pages consumed by pool_a alone (want 2): 2
cross-chunk values distinct and unaliased: true
stale handle rejected immediately after remove: true
stale handle still rejected after reuse (proves generation, not mere occupancy, gates the check): true
double free of a2's already-freed slot counted: true
pool_c hit its own MAX_CHUNKS ceiling while the page provider still had a page free: true
pages consumed after pool_a+pool_c+pool_b (want 4): 4
pool_d chunks active before first ensure (want 0): 0
pool_d ensure(1) after ensure(0) grows nothing new (idempotent): true
pool_d chunks active after crossing a chunk boundary (want 2): 2
pool_d cross-chunk values distinct, and slot 2 (never written, same fresh chunk as slot 3) reads back zeroed: true
pool_d ensure(index >= N) is rejected AS out-of-range, without touching the page provider: true
pool_e chunks active after ensure(1) alone (want 2): 2
pool_f ensure(0) consumed exactly one more page, now fully exhausting the shared provider: true
pool_f ensure(1) reports the PROVIDER out of memory, not its own chunk ceiling (provider, own ceiling, chunks < MAX): true false true
done
