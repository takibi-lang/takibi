schedule contention stage: unlocked
schedule contention stage: locked
schedule contention: two cores claiming one Ready process, the race shown without the lock and gone with it ok
tag contention stage: complete
tag contention: two cores minting a pool tag, every one advanced the counter once ok
pid contention stage: unlocked
pid contention stage: locked
pid contention: two cores minting, every pid advanced the counter once ok
fd refcount contention stage: complete
fd refcount contention: two cores retaining and releasing one object, count back to baseline ok
asid contention stage: complete
asid contention: two cores assigning, every number advanced the counter once ok
page contention stage: complete
page contention: two cores allocating and freeing, every page back, none freed twice ok
freelist contention stage: unlocked
freelist contention stage: locked
freelist contention: unlocked hands one index to both cores, a caller's lock stops it ok
pool contention stage: complete
pool contention: two cores cycling one pool, every allocation given back, no handle refused ok
occupancy drain stage: complete
occupancy drain: the second core held the region and the bounded drain gave up, then left and the same drain handed over a proof ok
world stop counts: full=1 partial=1 resumed=1 inspections=1 repeated=16 busy=16 stale-refused=1
world stop: full token stopped and resumed the online peer, partial token refused a non-acknowledging peer ok
network init contention counts: unlocked=2 locked=1
network init contention: unlocked check-set minted twice, typed one-shot gate admitted one initializer ok
tcp owner contention counts: busy=1 advanced=1
tcp owner contention: peer excluded while held and one state transition completed after release ok
pool walk stage: complete
pool walk: a Live view holds the read lock through payload use, and a stale released-chunk cursor is refused ok
