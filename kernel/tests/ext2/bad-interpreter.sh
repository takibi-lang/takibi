#!/bin/no-such-interpreter
# The interpreter is the thing that does not exist, not this file. Linux
# reports the interpreter's own ENOENT rather than a complaint about the
# script, and this fixture is what holds this kernel to that.
