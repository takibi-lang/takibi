# Claude Code Compatibility

This repository uses `AGENTS.md` as the canonical agent guidance file so
Claude Code, Codex, and other coding agents share the same starting point.

Read `AGENTS.md` in this directory first. More specific `AGENTS.md` files in
subdirectories override or extend the root guidance for work under those
directories.
